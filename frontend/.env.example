# Copy to .env.local for local dev. Vite reads VITE_* prefixed vars.
# Production values live in Vercel's Environment Variables settings.

# Where the workbench should fetch entities, audit, lineage, decisions.
# Defaults to http://localhost:8000 when unset.
VITE_API_BASE=http://localhost:8000
